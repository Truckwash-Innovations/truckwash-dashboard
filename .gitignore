node_modules
dist
release
.DS_Store
*.local

# Native builds (de projectmappen android/ en ios/ horen er WEL in)
android/.gradle
android/app/build
android/build
android/local.properties
android/.idea
android/app/release
ios/App/Pods
ios/App/build
ios/App/App/public
ios/DerivedData
*.xcuserstate

# Ondertekening -- nooit in git
*.keystore
*.jks
*.p12
*.mobileprovision

# gebouwde APK in de projectmap
Truckwash1-Dashboard-*.apk

# geheimen
.env
.env.local
.env.*.local

# Werkbestanden van de Supabase-CLI
supabase/.temp/

# Persoonlijke Claude Code-instellingen: hierin staan absolute paden van deze
# machine, en deze repo is publiek.
.claude/settings.local.json

# Uitvoer van analysegereedschap, geen broncode.
graphify-out/

# Werkstroomscripts van deze sessie, geen broncode.
.werkstroom/

# De werkmap van scripts/site-bouwen.cjs. Bestaat alleen tijdens een herbouw
# (en bij --droog ook erna); site/ is de uitkomst en die gaat wel mee.
.site-bouw/

# De momentopname die site-bouwen.cjs bij elke herbouw ophaalt. Hoort niet in
# de repo: een oude kopie is precies waar de versheidscontrole in
# scripts/site-bouwen.cjs een rem op zet.
sitebouw/vestigingen.json

# Testfacturen. Uitvoer van scripts/testfacturen.mjs, en er staan nummers van
# Truckwash zelf op zodra die in dat script worden ingevuld.
testfacturen/
